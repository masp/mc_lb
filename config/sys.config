[
    {mc_lb, []},
    {kernel, [
        {logger_level, notice},
        {logger, [
            {handler, default, logger_std_h, #{
                formatter =>
                    {logger_formatter, #{
                        depth => 50
                    }}
            }}
        ]}
    ]}
].
